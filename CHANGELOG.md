# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.5.0] - 2026-09-25

### 新增
- 信息中心卡片的标题与正文自动识别网址（`http://`、`https://`、`www.` 开头）并转为可点击链接，点击新标签页打开；紧跟网址后的中文与中英文标点不会被误拼进链接，未配对的右括号按正文标点剥离
- 「投票表决」独立模块：班委选举、评优表决、事项表决。发起人设置候选选项、资格名单（从班级名单选择或直接粘贴）与每人最多可选数；同学打开 `/v/投票ID` 免登录投票，重名需学号确认，名单外无法提交；一人一票（重投=改票），截止或手动停止后锁定；实时票数与占比、已完成 / 未投统计与未投名单一键复制提醒；匿名投票下选票明细仅发起人可查，记名投票对所有人公开；支持每人一条专属链接（`?u=` 锁定身份），停止 / 删除 / 明细接口支持管理令牌 `?t=` 委托调用
- 投票可选「需学号验证」：发起时勾选后，投票必须填写与名单一致的本人学号才能提交，进一步防冒名
- 未设管理密码时，信息页常驻安全提醒横幅（说明风险与设置方法，可一次性关闭）

### 安全
- 管理凭证与投递令牌拆分：新增自动生成的 `apiToken`（写入 config.json），`X-Token` 管理通行改用它；`ingestToken` 仅用于投递通知，泄露不再等于管理员泄露（注意：此前以 ingestToken 作 X-Token 的自动化脚本需改用 apiToken）
- 新增敏感操作审计日志 `data/logs/audit.log`：删除信息、名单增删改、投票编辑 / 停止 / 删除、备份导出 / 导入、登录成功等动作记录来源 IP，便于多人使用时追责

### 修复
- 修复匿名投票的学生页在设密码部署下不可用的问题：资格名单作为公示名单对所有人可见（学生页身份联想与专属链接锁定依赖它），选票明细仍仅发起人可查
- 备份导出 / 导入补齐投票与选票数据（此前恢复会静默丢失全部投票），空库判定同步覆盖投票表
- 投票详情页轮询定时器改为先清后设，长开页面不再堆积请求；投票截止 / 停止后学生页及时切换为结果态；发起人手动停止显示「已停止」而非「已结束」

### 变更
- 「名单库」更名为「班级名单」（v1.4.3 中该功能以名单库之名发布，功能不变仅名称调整）

### 清理
- 删除无引用的 `.sec-overdue` 样式规则与投票页内 `.badge.warn` / `.opt .ovotes` 未使用样式；自检脚本移除只写不读的附件跟踪数组（附件随信息级联删除）

## [1.4.3] - 2026-09-21

### 新增
- 「班级名单」独立视图：随时新建 / 编辑 / 重命名 / 删除 / 复制名单，成员实时预览，不再只能在发起接龙等场景顺手保存；名单含学生学号姓名，视图与接口仅管理员可见（访客隐藏入口，设密码部署下接口返回 401）
- 手机「添加到主屏幕」适配完善：manifest 补 192/512 PNG 与 maskable 图标（由原有 SVG 栅格化生成，零依赖不变），各页面补 apple-touch-icon 与 iOS 全屏 meta；符合条件的环境（HTTPS + Chromium 内核）顶栏出现「安装到桌面」一键安装入口；README 新增 Android / iOS 添加步骤说明

### 安全
- 修复设密码部署下 `GET /api/export/`（尾斜杠）绕过登录门禁、未认证下载全量备份的问题；`/api/inbox/` 同类绕过一并封堵（门禁改为归一化路径同判）
- 页面响应增加基础 CSP：允许内联脚本/样式（零构建所依赖），其余来源锁定本站，作为转义遗漏时的第二道防线
- 备份导入的 mime 字段做白名单校验，不再原样写进附件响应头

### 修复
- 信息中心卡片正文不再固定截断 150 字：正文完整渲染，超长部分默认折叠约 8 行，点「展开全文 / 收起」随时查看全部（访客同样可用）
- 接龙重名学生用裸姓名再次提交时不再静默覆盖同名的另一个人：学生页提前拦截，服务端对无法确定身份的提交返回 409（从下拉选带学号的一项后可正常更新）；服务端对客户端传来的槽位 rid 增加姓名核对，名单编辑期间不再记错人
- 名单解析：`1 张三 2023001`（空格序号）不再把学号绑到序号上，`2023001 张三 2023002 李四`（一行多对）正确两两配对，学号不再被当成姓名入库
- `limit` 传负数不再绕过分页上限（SQLite 的 `LIMIT -1` 等于不限条数）
- .ics 导出的 DTSTAMP 补齐 RFC 5545 要求的 T 分隔符，部分严格日历客户端不再拒收
- 接龙管理台 5 秒轮询与「返回」的竞态不再卡住视图；各视图加载器增加代次守卫，慢的旧响应不再覆盖新界面；信息流「加载更多」响应前连点不再重复追加；「已完成」区与文件中心的分页追加在视图重渲染后丢弃过期响应
- 当天生日横幅在「加载更多」后不再重复插入，首屏竞态下也能正常补插；弹窗取消后暂存附件即作废，关闭/刷新页面不再误报未保存
- 智能解析：已过的斜杠日期与"X月X日"顺延一年（12 月说"1/5 前交"不再解析到 11 个月前）；「晚上12点 / 上午12点」按午夜解析、「今晚8点」按 20 点解析；标题清洗不再剥掉日期开头的数字（`9月20日春游` 不再变成 `月20日春游`）
- 删除信息/附件时文件被占用不再静默留孤儿：启动与每日清扫任务移除无数据库引用的附件文件；自动备份先写临时文件再改名，中断不会留下顶掉好备份的半截文件；多文件上传中途失败自动回滚已写盘文件
- 自检脚本不再全表清理已有数据：名单 / 生日清理改为「导入前快照对比，只删自检创建的行」，相关空表断言同步收敛（此前在带真实数据的库上跑自检会误删名单与生日）

### 变更
- 信息中心「已完成」区改为按需加载：勾选「显示已完成」后只先显示总数，点「加载已完成」取最近 100 条，再点「显示更多」可逐页翻到最早一条（此前一次固定取 100 条，更早的看不到）
- 文件中心同样改为按需加载 + 「显示更多」分页：`/api/files` 新增 `offset` 参数与 `total` 总数字段（缺省行为不变）
- 文件中心跟随信息的完成状态：默认只显示未完成信息的附件，勾选「显示已完成」后，已完成信息的附件像信息中心一样单独成区展示在列表下方（带「已完成」标记）；`/api/files` 新增 `status=open|done` 参数（缺省仍返回全部）
- 搜索状态下两个列表直接加载第一页，不多一步点击
- `POST /api/import?force=1` 语义明确为合并导入：接龙/抽签按原 id 替换（条目先清后插，重导同一份备份结果一致），群按 ext_key 合并，名单同名覆盖，生日同名同日跳过；此前重导含接龙/抽签的备份必然撞唯一索引整体回滚
- Docker 镜像固定 `TZ=Asia/Shanghai`，容器内截止时间判定不再与北京时间差 8 小时
- Service Worker 对 js/css 改为 network-first（断网回落缓存）：部署新版本后首次刷新即是新脚本，不再出现新旧错配
- OneBot 接入：auto 模式私聊消息与群文件通知补上 5 分钟补发去重；按群名绑定站内群时只认领未绑定过其它 QQ 群的，防止两个群来回抢占

## [1.4.2] - 2026-09-18

### 修复
- 导入 JSON 备份时保留附件的阅读 / 下载计数（此前导出含计数但导入丢弃，roundtrip 后归零）
- 导入接龙记录时不再把超长 values_json 硬截断成非法 JSON（改为整条跳过，避免读取时静默丢内容）
- 「仅空库可导入」守卫改为检查全部业务表：只有名单 / 生日等数据时不再被误判为空库而重复导入
- 畸形 URL 参数（如未闭合的 `%` 编码）返回 400 而非 500，与 Express 路由行为一致
- 修复信息弹窗「重复录入检测」慢响应覆盖新结果的竞态（序号守卫，与命令面板同款）；快速录入页解析预览同
- 修复接龙页自动刷新后滚动位置丢失、刷新异常时静默标志不复位的问题
- 删除从未被读取的前端缓存字段 `state.cache`

### 安全
- 页面响应补 `X-Frame-Options: DENY` 与 `Referrer-Policy: same-origin`（对照 OWASP Secure Headers）
- 登录失败限速表每小时清扫 10 分钟无新失败的记录，公网部署下不再无限增长

### 清理
- 删除前端未使用的 6 个内联图标与 4 组无引用的 CSS 规则；合并被后文覆盖的重复声明（卡片悬停阴影、日历/勾选过渡等）
- 服务端收敛重复逻辑：附件内联预览白名单与截止时间比较口径提取为常量；webhook 接入、机器人自动收录、待审核收录三条路径合并为同一个「智能解析 + 入库」助手
- 统计页不再重复请求会话状态

## [1.4.1] - 2026-09-17

### 新增
- 命令面板（`Ctrl/⌘ + K`）：视图跳转、常用操作、信息实时搜索，完整键盘导航
- 键盘快捷键体系：`/` 聚焦搜索、`N` 添加信息、`1-9` 切换视图、`?` 帮助（`Ctrl+Enter` 保存原有）
- 前端内联 SVG 图标体系（约 50 个，零依赖），替换界面全部功能型 emoji
- 信息流骨架屏加载、空状态圆形托底、弹窗入场动画与焦点圈定（Tab 循环 + 焦点归还）
- 移动端底部导航收敛：5 个高频视图 + 「更多」展开面板
- 搜索框 `/` 键位提示、截止时间 Chip 图标、深色模式面板描边、复选框主题色
- Toast 图标化 + `aria-live`；图标按钮补齐 `aria-label`；导航活动态 `aria-current`
- GitHub Actions CI（Node 22 / 24 矩阵：语法检查 + 端到端自检）
- API JSON 响应 gzip 压缩（node:zlib，大于 1KB 自动启用，列表体积可降 70%+）
- 健康检查返回版本号与运行时长；当天消息显示"刚刚 / N 分钟前"相对时间
- PWA 长按快捷方式（快速记录）；登录页已登录自动跳回主界面；接龙名单 / 生日页打印样式

### 变更
- 深色模式下生日贺卡改为深色系渐变（移除浅色纸卡的割裂感），生日页背景氛围低调化
- 分类配色降饱和；圆角收敛为 `--radius-sm/md/lg` 阶梯；标题排版层级拉开
- 学生接龙页、快速录入页、登录页与主界面图标/主题按钮统一
- 二维码组件改为按需懒加载（首页不再加载 qrcode.min.js）
- `theme-color` 跟随浅色 / 深色主题，手机状态栏颜色同步

### 修复
- 安全：附件缩略图接口 `/raw` 限制内联类型为位图/PDF，其余类型强制下载（修复 HTML/SVG 借此同源内联渲染的风险）
- 安全：接龙 CSV 导出对以 `= + - @` 开头的单元格加 `'` 前缀中和，防 Excel 公式注入
- 安全：班徽响应增加 `Content-Security-Policy: sandbox`，内嵌脚本的 SVG 直接打开也不执行
- 修复 OneBot 人工审核模式下，相同内容跨群上报被误判为重复的问题
- 修复铃铛角标被通知状态刷新覆盖、快速录入页只读访客无法跳转登录的问题
- 弹窗未保存提醒覆盖全部表单（原仅信息编辑）；命令面板 Tab 焦点圈定；清理死代码
- 修复移动端「更多」按钮被通用导航绑定误捕获、跳到统计页的问题
- 修复二维码尺寸 CSS 选择器失效（`#qr-modal` 不存在导致规则从未命中）
- 修复抽签按钮 loading 态吞掉图标的回归

### 安全
- 接入令牌、OneBot 令牌、接龙管理令牌的字符串比较全部改为常量时间比较（`timingSafeEqual`）
- 管理会话增加 30 天 TTL（惰性清除 + 每小时批量清理），不再无限占用内存
- 支持 `SIGTERM` 优雅退出（systemd / Docker 停止时先排空连接）
- `test.js` 附件统计断言改为基线增量，非空数据库也能通过

## [1.4.0] - 2026-09-14

### 新增
- 生日祝福模块：全班生日倒计时贺卡墙、当天祝福墙（极光背景 / 彩旗 / 气球动画）、生日横幅、浏览器通知、导航弹跳角标、一键复制祝福、班徽水印背景
- 生日只存月日（年份选填显示"将满 N 岁"）；2 月 29 日平年按 2 月 28 日庆祝
- 三档外观：浅色 / 深色 / 自动（19:00–次日 7:00 深色）

## [1.3.0] - 2026-09-13

### 新增
- 抽签点名：按名单公平轮抽，抽过自动排除、抽空自动开新一轮，Fisher-Yates + `crypto.randomInt` 无偏随机，支持撤销 / 重置 / 历史留痕
- 班级名单：名单存一份，接龙、抽签、生日导入到处复用

## [1.2.0] - 2026-09-13

### 新增
- 班级接龙（独立项目「接龙小助手」并入）：链接即填即交、名单自动比对谁没接龙、重名按学号区分、名单外标记、专属链接防代填、管理令牌委托班委、CSV 导出（带 BOM）、扫码接龙

## [1.1.0] - 2026-09-13

### 新增
- OneBot 11 QQ 机器人接入：HTTP POST 上报自动进站，人工审核 / 自动收录两种模式，防闲聊过滤、群白名单、HMAC 签名校验、5 分钟重复去重
- 待审核收件箱：收录 / 忽略 / 全部收录，自动识别分类与截止时间
- 附件阅读 / 下载次数统计（缩略图渲染不计阅读）

## [1.0.0] - 2026-09-13

### 新增
- 首个正式版本：信息看板（通知 / 任务 / 活动 / 文件分色）、智能解析中文通知、待办任务分桶、日历视图、文件中心、附件预览与计数、`.ics` 日历导出、Webhook 接入、JSON 导入导出、每日自动备份、可选管理密码、PWA
