# 安全策略

## 支持的版本

| 版本 | 支持情况 |
| --- | --- |
| 1.4.x | ✅ 支持 |
| < 1.4 | ❌ 建议升级 |

## 报告漏洞

请**不要**通过公开 Issue 报告安全漏洞。使用 GitHub 的"私下报告漏洞"功能（仓库 Security 标签页 → Report a vulnerability），或通过仓库主页提供的联系方式联系维护者。

报告时请尽量附上：触发路径 / 复现步骤、影响范围、可能的修复思路。我们会在收到后尽快回应，修复前不公开细节。

## 安全模型说明

了解本项目的安全边界有助于评估问题的严重程度：

- **默认信任本机 / 局域网**：未设置管理密码时，所有能访问服务端口的人都可以读写数据。这是"家庭局域网自用"的设计取舍，公网部署务必设置密码（`data/config.json` 的 `password` 字段）。
- **所有人可浏览**：即使设置了密码，信息内容对学生 / 家长访客仍是公开可读的（设计如此，方便班级浏览），密码只保护增删改。
- **令牌即权限**：`ingestToken`（Webhook / 机器人 / X-Token 通行）与接龙管理令牌等同密钥，泄露后修改配置重启即可作废。
- **敏感数据本地化**：学生名单、生日等数据全部保存在本机 `data/` 目录，请勿将该目录公开或提交到仓库（已在 `.gitignore` 排除）。
